<html>
    <head>
        <link rel="stylesheet" href="./styles.css">
        <title><%=locals.title %></title>
    </head>
    <body>
        <ul>
            <% messages.forEach(message => {%>
            <%-include("initial",{message})  %>
        <% })  %>
        </ul>
        <a href="/new">
            <button>Create message</button>
        </a>
    </body>
</html>